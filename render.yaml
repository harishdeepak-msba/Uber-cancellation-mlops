services:
  - type: web
    name: uber-cancellation-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.fastapi_app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
